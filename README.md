# Risc V Processor Implementation 

Requirements: 
    iverilog
    gtkwave

To compile and view waveforms in GTKwave, run the *run* script
inside of /processor. 
You might need to install WSL (windows subsystem for linux) and navigate 
to the directory there.
